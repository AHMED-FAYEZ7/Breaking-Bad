
import 'package:flutter/material.dart';

class CharactarScreen extends StatelessWidget {
  const CharactarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Ahmed"),
    );
  }
}
